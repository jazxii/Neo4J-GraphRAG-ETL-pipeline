# ── Neo4j ETL Pipeline ──
neo4j>=5.0.0
python-dotenv>=1.0.0

# ── WCAG Enrichment Scraper ──
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Agentic RAG ──
openai>=1.0.0          # LLM client (also works with Ollama/Anthropic via base_url)
